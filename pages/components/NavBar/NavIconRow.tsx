import React from 'react';
import styled from "styled-components";

const StyledContainer = styled.div`
  background: #424242;
  border-radius: 6px;
  height: 40px;
  width: 220px;
`;
type NavIconRowProps = {
    imgPath: string;
    text: string;
};

const NavIconRow = () => {
  return (
    // <img></img>
    <div>NavIconRow</div>
  )
}

export default NavIconRow;